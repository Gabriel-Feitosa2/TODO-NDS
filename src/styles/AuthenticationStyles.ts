import { createGlobalStyle } from 'styled-components';

const AuthenticationStyles = createGlobalStyle`
    body {
        margin:0;
        background-color: #25294c;
    }
`;

export default AuthenticationStyles;
