import styled, { createGlobalStyle } from 'styled-components';

export const TarefasStyles = createGlobalStyle`
  body {
    background-color: #E9ECEF;
  } 
`;

export const TarefaDashBoard = styled.div`
  position: absolute;
  inset: 0rem 0rem 0rem;
`;
